<%= render @task %>

<div class="mt-2">
  <%= link_to t("actions.edit"), edit_task_path(@task), class: "link" %> |
  <%= link_to t("actions.back"), tasks_path, class: "link" %>

  <%= button_to t("actions.destroy"), @task, method: :delete, class: "btn btn-danger mt-2" %>
</div>
